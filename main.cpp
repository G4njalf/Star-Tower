#include"map.hpp"

int main()
{
    //bool exit = menu(); // commenta sotto per menu e usa questo
    bool exit = false;
    if (exit){
        endwin();
    }
    else{
        map(exit);
    }
}